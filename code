.reduce((unique, row) => {
  // Use a more accurate key: PersonGUID + PersonnelType + CardNumber
  const keyGUID = `${row.PersonGUID}-${row.PersonnelType}-${row.CardNumber}`;
  if (!unique._seen.has(keyGUID)) {
    unique._seen.add(keyGUID);
    unique.rows.push(row);
  }
  return unique;
}, { _seen: new Set(), rows: [] })
.rows;