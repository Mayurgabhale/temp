useEffect(() => {
  setLoading(true);
  fetchHistory(decodedPartition)
    .then(json => {
      console.log("Fetched data:", json);
      setData(json);
    })
    .finally(() => setLoading(false));
}, [decodedPartition]);






