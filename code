<div class="card">
  <h3 class="camera-header">
    <div class="camera-wrapper">
      <img src="images/camera.png" alt="Camera" class="animated-camera" />
      <span class="status-led online"></span> <!-- or "offline" -->
    </div>
  </h3>
  <div class="card-status total">Total <span id="camera-total">0</span></div>
  <div class="card-status online">Online <span id="camera-online">0</span></div>
  <div class="card-status offline">Offline <span id="camera-offline">0</span></div>
</div>



/* Wrap for alignment */
.camera-wrapper {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  gap: 10px;
}

/* Moving camera animation */
.animated-camera {
  height: 32px;
  animation: floatCamera 3s ease-in-out infinite alternate;
}

/* Blinking LED */
.status-led {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  display: inline-block;
  box-shadow: 0 0 8px rgba(0,0,0,0.5);
}

/* Blinking animation */
.status-led.online {
  background-color: #2cb67d;
  animation: blinkGreen 1.2s infinite;
}

.status-led.offline {
  background-color: #ef4565;
  animation: blinkRed 1.2s infinite;
}

/* Keyframes */
@keyframes floatCamera {
  0% { transform: translateX(-8px); }
  100% { transform: translateX(8px); }
}

@keyframes blinkGreen {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.2; }
}

@keyframes blinkRed {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.1; }
}