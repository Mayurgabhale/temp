––– src/pages/History.jsx
@@ const detailRows = useMemo(() => {
-  return data.details
-    // …
-    .filter(r => r.Direction === 'InDirection')    // ❌ drops half your events
+  return data.details
+    // … (other filters stay the same)
+    // (remove the Direction filter entirely so it matches summary's logic)

@@ .reduce((unique, row) => {
-    const keyGUID = `${row.PersonGUID}-${row.PersonnelType}`;
+    const keyGUID = row.PersonGUID;                 // de-dupe on GUID only