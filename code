/* Fix the footer at bottom */
footer {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 50px; /* adjust as needed */
  background: #f1f1f1;
  text-align: center;
  line-height: 50px;
  border-top: 1px solid #ccc;
  z-index: 1000;
}

/* Main content area should not be blocked by fixed footer */
.main-content {
  padding-bottom: 60px; /* make space for fixed footer */
}

/* Scrollable table area */
.table-container {
  max-height: calc(100vh - 200px); /* adjust if needed */
  overflow-y: auto;
  margin-bottom: 1rem;
}