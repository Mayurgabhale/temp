body {
  display: flex;
  flex-direction: column;
  height: 100vh;
  margin: 0;
}

.main-layout {
  display: flex;
  flex: 1;
  overflow: hidden;
}


.container {
  flex: 1;
  overflow-y: auto;
  max-height: unset; /* override previous max-height */
}