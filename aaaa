import axios from 'axios';

const BASE = process.env.REACT_APP_API_BASE || 'http://localhost:3007/api/occupancy';

export const fetchLiveSummary = async () => {
  const response = await axios.get(`${BASE}/live-summary`);
  return response.data;
};