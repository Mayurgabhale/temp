const renderData =
  total === 0 && showZeroSlice
    ? [{ name: 'Available', value: 1 }] // to force a visible slice
    : data;

...

<Pie
  data={renderData}
  dataKey="value"
  nameKey="name"
  innerRadius={70}
  outerRadius={120}
  paddingAngle={4}
  isAnimationActive
  animationDuration={animationDuration}
  labelLine={false}
  label={({ percent, value }) => (value > 0 ? `${(percent * 100).toFixed(0)}%` : '')}
>