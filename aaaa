<Pie
  data={renderData}
  dataKey="value"
  nameKey="name"
  innerRadius={70}
  outerRadius={120}
  paddingAngle={4}
  isAnimationActive
  animationDuration={animationDuration}
  labelLine={false}
  label={({ percent }) => (percent > 0 ? `${(percent * 100).toFixed(0)}%` : '')}
>