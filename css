<!-- Custom Alert Modal -->
<div id="custom-alert" class="custom-alert">
  <div class="custom-alert-content">
    <div class="custom-alert-header">
      <span id="custom-alert-title">Notification</span>
      <div class="custom-alert-controls">
        <button onclick="minimizeAlert()">_</button>
        <button onclick="closeAlert()">×</button>
      </div>
    </div>
    <div class="custom-alert-body" id="custom-alert-body"></div>
  </div>
</div>





.custom-alert {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 9999;
  background-color: #fff;
  border: 2px solid #333;
  border-radius: 10px;
  width: 360px;
  box-shadow: 0 0 20px rgba(0,0,0,0.3);
  font-family: sans-serif;
}

.custom-alert-content {
  display: flex;
  flex-direction: column;
}

.custom-alert-header {
  background: #f5f5f5;
  padding: 10px;
  font-weight: bold;
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: move; /* draggable handle (optional) */
}

.custom-alert-controls button {
  margin-left: 5px;
  cursor: pointer;
  border: none;
  background: none;
  font-size: 16px;
}

.custom-alert-body {
  padding: 15px;
  white-space: pre-line;
}

.minimized .custom-alert-body {
  display: none;
}





// === ALERT HELPER FUNCTIONS ===

function notifyWindows(title, message) {
  const alertBox = document.getElementById('custom-alert');
  document.getElementById('custom-alert-title').textContent = title;
  document.getElementById('custom-alert-body').textContent = message;
  alertBox.classList.remove('minimized');
  alertBox.style.display = 'block';
}

function closeAlert() {
  document.getElementById('custom-alert').style.display = 'none';
}

function minimizeAlert() {
  document.getElementById('custom-alert').classList.toggle('minimized');
}

// === EXISTING LOGIC YOUR CODE ===
// (insert your device timers, startDowntime, startUptime, etc. here)

...
// Replace original notifyWindows calls with this new modal-based function.
// Example inside startDowntime:
if (secs >= 180 && !deviceOfflineAlerted[safe]) {
  deviceOfflineAlerted[safe] = true;
  const title = "⚠️ Device Offline ≥ 3 min";
  const message = /* build your multi-line string */;
  notifyWindows(title, message);
}
...





function notifyAll(title, message) {
  // Native system notification:
  if (Notification.permission === 'granted') {
    new Notification(title, { body: message });
  } else {
    Notification.requestPermission().then(p => {
      if (p === 'granted') new Notification(title, { body: message });
    });
  }
  // Styled, centered browser modal:
  notifyWindows(title, message);
}