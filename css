const puneFloors = realtimeData?.['IN.Pune']?.floors ?? {};
const puneDetails = realtimeData?.details ?? [];

const puneEnriched = enrichFloorsWithTypes('IN.Pune', puneFloors, puneDetails);

<CompositeChartCard
  title="Pune Floor Breakdown"
  data={puneEnriched}
/>