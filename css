
            <div class="summary">
                <div class="card">
                    <h3><i class="fas fa-microchip"></i> Total Devices</h3>
                    <div class="card-status total">Total <span id="total-devices">0</span></div>
                    <div class="card-status online">Online <span id="online-devices">0</span></div>
                    <div class="card-status offline">Offline <span id="offline-devices">0</span></div>
                </div>

                <div class="card camera-card">
                    <model-viewer src="camera.glb" alt="3D Security Camera" auto-rotate camera-controls disable-zoom
                        class="moving-camera" style="width: 100%; height: 220px; background: transparent;">
                    </model-viewer>

                    <div class="card-status total">Total <span id="camera-total">0</span></div>
                    <div class="card-status online">Online <span id="camera-online">0</span></div>
                    <div class="card-status offline">Offline <span id="camera-offline">0</span></div>
                </div>




/* Make the card float left to right */
.moving-camera {
  animation: moveSideways 5s ease-in-out infinite alternate;
  display: block;
  margin: 0 auto;
}

/* Define the animation */
@keyframes moveSideways {
  0% { transform: translateX(-10px); }
  100% { transform: translateX(10px); }
}

.camera-3d-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 160px;
  margin-bottom: 10px;
}

.camera-3d-container {
  width: 220px;
  height: 140px;
  position: relative;
  animation: cameraFloat 4s ease-in-out infinite alternate;
}

.camera-3d-container iframe {
  width: 100%;
  height: 100%;
  border-radius: 10px;
  pointer-events: none;
}

.status-led {
  position: absolute;
  bottom: 8px;
  right: 8px;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  box-shadow: 0 0 8px rgba(0, 0, 0, 0.4);
}

.status-led.online {
  background-color: #2cb67d;
  animation: blinkGreen 1.2s infinite;
}

.status-led.offline {
  background-color: #ef4565;
  animation: blinkRed 1.2s infinite;
}

@keyframes cameraFloat {
  0% { transform: translateX(-10px); }
  100% { transform: translateX(10px); }
}

@keyframes blinkGreen {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

@keyframes blinkRed {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.1; }
}
