.card-content {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding-left: 4px;
}

/* Style each line (p tag) to align icon and text */
.card-content p {
  display: flex;
  align-items: center;
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  font-size: 0.95rem;
}

/* Style icons inside strong tags */
.card-content p strong i {
  margin-right: 10px;
  min-width: 18px;
  text-align: center;
  color: #00adb5;
}

/* Optional: make icons slightly larger */
.card-content p i {
  font-size: 1.05rem;
}

/* Align span text neatly with consistent color */
.card-content p span {
  font-weight: 600;
  color: #2d2d2d;
}