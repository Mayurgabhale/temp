<div class="card camera-card">
  <div class="camera-3d-wrapper">
    <iframe
      title="Security Camera"
      frameborder="0"
      allowfullscreen
      mozallowfullscreen="true"
      webkitallowfullscreen="true"
      allow="autoplay; fullscreen; xr-spatial-tracking"
      src="https://sketchfab.com/models/c22374e8095149b794e821c9ec0a796e/embed"
      class="camera-3d-iframe">
    </iframe>
    <span class="status-led online"></span>
  </div>

  <div class="card-status total">Total <span id="camera-total">0</span></div>
  <div class="card-status online">Online <span id="camera-online">0</span></div>
  <div class="card-status offline">Offline <span id="camera-offline">0</span></div>
</div>





/* Container for animation */
.camera-3d-wrapper {
  width: 100%;
  max-width: 250px;
  margin: 0 auto 10px;
  position: relative;
  animation: cameraFloat 4s ease-in-out infinite alternate;
}

/* 3D iframe styling */
.camera-3d-iframe {
  width: 100%;
  height: 220px;
  border: none;
  border-radius: 12px;
  pointer-events: none; /* makes it non-clickable */
}

/* LED status light */
.status-led {
  position: absolute;
  bottom: 12px;
  right: 12px;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  box-shadow: 0 0 8px rgba(0, 0, 0, 0.4);
}

.status-led.online {
  background-color: #2cb67d;
  animation: blinkGreen 1.2s infinite;
}

.status-led.offline {
  background-color: #ef4565;
  animation: blinkRed 1.2s infinite;
}

/* Float left-right animation */
@keyframes cameraFloat {
  0% { transform: translateX(-10px); }
  100% { transform: translateX(10px); }
}

/* LED blink animation */
@keyframes blinkGreen {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

@keyframes blinkRed {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.1; }
}