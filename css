<Box
  sx={{
    gridRow: 2,
    position: 'sticky',
    top: 0,
    zIndex: 5,
    display: 'inline-flex',      // ✅ inline-flex so it shrinks to fit content
    justifyContent: 'center',
    alignItems: 'center',
    flexWrap: 'wrap',
    gap: 2,
    p: 2,
    mx: 'auto',                  // ✅ center the container horizontally
    backgroundColor: 'transparent', // ✅ no background
  }}
>