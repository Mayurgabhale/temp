label={({ percent, payload }) => {
  const employee = payload.Employee ?? 0;
  const contractor = payload.Contractor ?? 0;
  return `${payload.name}\n${(percent * 100).toFixed(0)}%\nEmp: ${employee} / Cont: ${contractor}`;
}}