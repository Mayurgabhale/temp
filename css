.header-left button {
  background: none;
  border: none;
  padding: 0;
  font-size: 20px;
  color: #000;
  cursor: pointer;
  transition: transform 0.2s ease;
}

.header-left button:hover {
  transform: scale(1.2);
  color: #333;
}