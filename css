<model-viewer
  src="images/camera.glb"
  alt="3D Security Camera"
  disable-zoom
  camera-controls
  auto-rotate="false"
  class="floating-camera"
  style="width: 100%; height: 200px; background: transparent;">
</model-viewer>



.floating-camera {
  animation: floatLeftRight 4s ease-in-out infinite alternate;
  display: block;
  margin: 0 auto;
  pointer-events: none; /* optional: disables user drag */
}

/* Smooth left-to-right floating */
@keyframes floatLeftRight {
  0% { transform: translateX(-15px); }
  100% { transform: translateX(15px); }
}