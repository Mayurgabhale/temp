<header className="header">
  <div className="header-content">
    <div className="header-logo-text">
      <img src={wulogo} alt="WU Logo" className="logo" />
      <div className="header-text">
        <h1>Associate Verification Tool</h1>
        <p>Pune Technology Engineering Center • Mon–Fri, 9:00–18:00</p>
      </div>
    </div>
  </div>
</header>




/* Header */
.header {
  background-color: var(--wu-black);
  color: var(--wu-yellow);
  padding: 1.2rem 1rem;
  border-bottom: 4px solid var(--wu-yellow);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
  position: sticky;
  top: 0;
  z-index: 1000;
}

/* Flex wrapper for full width */
.header-content {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

/* Logo and text side by side, aligned center */
.header-logo-text {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.logo {
  height: 48px;
  width: auto;
}

/* Text styles */
.header-text h1 {
  font-size: 1.6rem;
  font-weight: 700;
  margin: 0;
  letter-spacing: 0.4px;
  line-height: 1.2;
}

.header-text p {
  font-size: 0.95rem;
  margin: 0;
  opacity: 0.9;
}

/* Responsive styles */
@media screen and (min-width: 768px) {
  .header-content {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .header-text h1 {
    font-size: 2rem;
  }

  .header-text p {
    font-size: 1.05rem;
  }
}