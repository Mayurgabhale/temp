 {/* Markers */}
          {markers.map((m, i) => {
            const r = 1 * zoom;
            return (
              <Marker key={i} coordinates={m.coords} onClick={() => onClickSite(selected)}>
                <circle r={r} fill={m.color} stroke="#fff" strokeWidth={1}>
                  {m.tooltip && <title>{m.tooltip}</title>}
                </circle>
                {m.showLabel && (
                  <text
                    textAnchor="middle"
                    y={-r - 2}
                    fill={m.color}
                    fontSize={4 * zoom}
                    fontWeight="bold"
                  >
                    {m.name}
                  </text>
                )}
              </Marker>




                       in this page in circle i want to add this location icon <i class="bi bi-geo-alt"></i> means location point show this location icon dont make any change just add this loation icon carefllly
